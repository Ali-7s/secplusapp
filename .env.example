# Copy this file to .env and fill in your values (for local docker-compose only)
# Never commit .env to source control

# Your Anthropic API key — required
ANTHROPIC_API_KEY=sk-ant-your-key-here

# JWT signing secret — must be at least 32 characters, keep it secret!
JWT_SECRET=replace-with-a-long-random-secret-at-least-32-chars

# Frontend nginx proxy — MUST include http:// or https:// scheme
# Railway: set to your backend's public domain, e.g. https://backend-production-xxxx.up.railway.app
# Docker Compose: leave unset (defaults to http://backend:8080 via Dockerfile ENV)
# BACKEND_URL=https://your-backend.up.railway.app

# Optional — restrict CORS to your frontend domain (defaults to * if unset)
# CORS_ORIGINS=https://your-frontend.up.railway.app
